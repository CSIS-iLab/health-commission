---
label: Categories
hide_body: false
is_partial: true
fields:
- type: field_group_list
  name: themes
  label: Themes
  fields:
  - type: select
    name: theme
    config:
      source:
        type: pages
        section: themes
    label: Theme
- type: select
  name: series
  config:
    source:
      type: pages
      section: series
  label: Series
- type: list
  name: keywords
  label: Tags
  config:
    use_select: true
    source:
      type: pages
      section: tags
  default: []
