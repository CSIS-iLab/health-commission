---
label: Categories
hide_body: false
is_partial: true
fields:
- type: select
  name: series
  config:
    source:
      type: pages
      section: series
  label: Series
- type: list
  name: keywords
  label: Tags
  config:
    use_select: true
    source:
      type: pages
      section: tags
  default: []
- type: list
  name: themes
  label: Themes
  description: The theme listed first will be considered the "primary" theme.
  config:
    use_select: true
    source:
      type: pages
      section: themes
