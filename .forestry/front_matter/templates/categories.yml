---
label: Categories
hide_body: false
is_partial: true
fields:
- type: list
  name: themes
  label: Themes
  config:
    use_select: true
    source:
      type: pages
      section: themes
- type: select
  name: series
  config:
    source:
      type: pages
      section: series
  label: Series
- type: list
  config:
    use_select: true
    source:
      type: pages
      section: tags
  label: Tags
  name: tags
