---
label: Categories
hide_body: false
is_partial: true
fields:
- type: field_group_list
  name: themes
  label: Themes
  fields:
  - type: select
    name: theme
    config:
      source:
        type: pages
        section: themes
    label: Theme
- type: select
  name: series
  config:
    source:
      type: pages
      section: series
  label: Series
- type: tag_list
  name: tags
  label: Tags
- type: select
  config:
    source:
      type: datafiles
